version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - echo "VITE_API_ENDPOINT=$VITE_API_ENDPOINT" > .env
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
